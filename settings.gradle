pluginManagement {
  repositories {
    gradlePluginPortal()
    mavenCentral()
  }
  plugins {
    // Kotlin for core module (you can write Kotlin immediately).
    id 'org.jetbrains.kotlin.jvm' version '2.3.0'
  }
}

rootProject.name = 'projectbeats-gdx'

include('core', 'desktop', 'teavm')
